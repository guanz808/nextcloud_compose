version: '3.8' 

services:

#  proxy:
#    image: jwilder/nginx-proxy:alpine
#    labels:
#      - "com.github.jrcs.letsencrypt_nginx_proxy_companion.nginx_proxy=true"
#    container_name: nextcloud-proxy
#    networks:
#      - nextcloud_network
#    ports:
#      - 80:80
#      - 443:443
#    volumes:
#      - ./proxy/certs:/etc/nginx/certs:ro
#      - ./proxy/vhost.d:/etc/nginx/vhost.d:rw
#      - ./proxy/html:/usr/share/nginx/html:rw
#      - ./proxy/conf.d:/etc/nginx/conf.d:rw
#      - /etc/localtime:/etc/localtime:ro
#      - /var/run/docker.sock:/tmp/docker.sock:ro
#    restart: unless-stopped
#
#  letsencrypt:
#    image: jrcs/letsencrypt-nginx-proxy-companion
#    container_name: nextcloud-letsencrypt
#    depends_on:
#      - proxy
#    networks:
#      - nextcloud_network
#    volumes:
#      - ./proxy/certs:/etc/nginx/certs:rw
#      - ./proxy/vhost.d:/etc/nginx/vhost.d:rw
#      - ./proxy/html:/usr/share/nginx/html:rw
#      - /etc/localtime:/etc/localtime:ro
#      - /var/run/docker.sock:/var/run/docker.sock:ro
#    restart: unless-stopped

  db:
    image: mariadb
    command: --transaction-isolation=READ-COMMITTED --binlog-format=ROW --innodb-file-per-table=1 --skip-innodb-read-only-compressed
    container_name: nextcloud-mariadb
    networks:
      #- nextcloud_network
      - existingnetwork
    volumes:
      - db:/var/lib/mysql
      - /etc/localtime:/etc/localtime:ro
    environment:
      - MYSQL_ROOT_PASSWORD=$MYSQL_ROOT_PASSWORD
      - MYSQL_PASSWORD=$MYSQL_PASSWORD
      - MYSQL_DATABASE=nextcloud
      - MYSQL_USER=nextcloud
    restart: unless-stopped
  
  app:
    image: nextcloud:latest
    container_name: nextcloud-app
    networks:
      #- nextcloud_network
      - existingnetwork
    ports:
      - 80:80
      - 443:443      
    depends_on:
#      - letsencrypt
#      - proxy
      - db
    volumes:
      - entertainment:/entertainment
#      - /mediastorage/Entertainment_TV:/entertainment_tv
#      - /mediastorage/EverythingElse:/everythingelse
      - data:/var/www/html
      - /etc/localtime:/etc/localtime:ro
    environment:
      - VIRTUAL_HOST=www.dajays.com
#      - LETSENCRYPT_HOST=www.dajays.com
#     - LETSENCRYPT_EMAIL=YOUR-EMAIL
    restart: unless-stopped

volumes:
#  movies:
#    external: true
#  nextcloud:
#    external: true
#  nextcloud_db:
#    external: true
  data:
    driver: local
    driver_opts:
      type: nfs
      o: "addr=192.168.1.11,rw,noatime,rsize=8192,wsize=8192,tcp,timeo=14,nfsvers=4"
      device: ":/volume1/docker/nextcloud/data"
  db:
    driver: local
    driver_opts:
      type: nfs
      o: "addr=192.168.1.11,rw,noatime,rsize=8192,wsize=8192,tcp,timeo=14,nfsvers=4"
      device: ":/volume1/docker/nextcloud/db" 
  entertainment:
    driver: local
    driver_opts:
      type: nfs
      o: "addr=192.168.1.11,rw,noatime,rsize=8192,wsize=8192,tcp,timeo=14,nfsvers=4"
      device: ":/volume1/entertainment"           

networks:
  existingnetwork:
    external: true
    name: infrastructure_net 
  #nextcloud_network:
